import { about } from "../utils/tailwindClasses";

export default function AboutPage() {
    return (
        <div className={about}>
            <div>
                <h1>ABOUT PAGE!!!</h1>
            </div>
        </div>
    );
}