import ItemGrid from './ItemGrid'

export default function Dashboard({ className }) {
    return (
        <div className={className}>
            Dashboard
            <ItemGrid />
        </div>
    );
}